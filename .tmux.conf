# Reload config by using
# tmux source-file ~/.tmux.conf


# Editing
# --------------------
setw -g mode-keys vi			# Enable vi mode
set -s escape-time 0			# Remove delay on pressing escape

# Visual
# --------------------
set -g default-terminal "screen-256color"				# Make tmux use colors
set -g status-bg black			# Status bar bg
set -g status-fg white			# Status bar fg
#set inactive/active window styles
set -g window-style				'fg=default,bg=black'
set -g window-active-style		'fg=default,bg=black'

# Keybindings
# --------------------
# Note: the Option key is mapped as 8-bit when used as modifier
# to produce the special characters below. Default Option key
# will produce different chars and sometimes will act as accent
# modifier, preventing single press shortcut.
# bind -n C-q copy-mode	
# bind -n C-x kill-pane 
# bind -n C-3 select-pane -t :.-
# bind -n C-r select-pane -t :.+
# bind -n C-t split-window -h -c "#{pane_current_path}"		# Split right
# bind -n C-y split-window -v -c "#{pane_current_path}"		# Split below
# bind -n C-h resize-pane -L 5
# bind -n C-j resize-pane -D 5
# bind -n C-k resize-pane -U 5
# bind -n C-l resize-pane -R 5
# bind -n œ copy-mode             # Opt-q
# bind -n ≈ kill-pane             # Opt-x
# bind -n ˙ resize-pane -L 5      # Opt-h
# bind -n ∆ resize-pane -D 5      # Opt-j
# bind -n ˚ resize-pane -U 5      # Opt-k
# bind -n ¬ resize-pane -R 5      # Opt-l
# bind -n ∂ select-pane -t :.-    # Opt-d
# bind -n ƒ select-pane -t :.+    # Opt-f
# bind -n ® split-window -h -c "#{pane_current_path}"		# Opt-r Split right
# bind -n ´ split-window -v -c "#{pane_current_path}"		# Opt-e Split below
