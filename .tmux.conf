# Reload config by using
# tmux source-file ~/.tmux.conf

set -g default-terminal "screen-256color"	# Make tmux use colors
set-window-option -g mode-keys vi			# Enable vi mode
set -s escape-time 0		# Remove delay on pressing escape
set -g prefix C-j			# Rebind prefix
set -g status-bg black		# Status bar bg
set -g status-fg white		# Status bar fg

bind -n ˚ resize-pane -U 5		# M-k resizes pane up
bind -n ∆ resize-pane -D 5		# M-j resizes pane down
bind -n ˙ resize-pane -L 5		# M-h resizes pane left
bind -n ¬ resize-pane -R 5		# M-l resizes pane right
bind-key x kill-pane			# Kill pane without confirmation (default keybinding)
